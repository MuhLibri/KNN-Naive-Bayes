[project]
name = "dont-mine-at-night"
version = "0.1"
dependencies = [
    "matplotlib",
    "pandas",
    "scikit-learn"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
