[project]
name = "dont-mine-at-night"
version = "0.1"
dependencies = [
    "pandas",
    "sklearn"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
